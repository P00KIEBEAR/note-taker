const router = require('express').Router();
const { response } = require('express');
const db = require('../db/helperFile');

/*
router.get('/notes', (req, res) => {
  db.getNotes()
    .then((notes) => res.json(notes))
    .catch((err) => res.status(500).json(err))

})*/


module.exports = router






















